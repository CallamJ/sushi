class Counter {
    int count

    new(int start) {
        this.count = start
    }

    int value() {
        return this.count
    }

    increment() {
        this.count += 1
    }
}

var counter = new Counter(7)
counter.increment()
println(counter.value())
