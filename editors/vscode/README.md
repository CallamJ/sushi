# Sushi for VS Code

Install Sushi on your `PATH`, install the generated VSIX, then open a `.sushi` file. Set `sushi.serverPath` when the executable is not named `sushi`; set `sushi.targetProfile` to control target-aware diagnostics.
